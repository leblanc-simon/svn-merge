[common]
base = "~/sd/example/www/"
svn = svn://localhost/example/
description = "An example project"

[preprod]
dir = branches/preprod/
from = trunk
message = "(trunk into preprod)"
description = "Merge some commit from trunk into preprod branch"

[prod]
dir = branches/prod/
from = branches/preprod
message = "(preprod into prod)"